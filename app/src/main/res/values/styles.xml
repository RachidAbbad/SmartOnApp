<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="PermissionBlueStyle">
        <item name="PermissionTitleColor">@color/uiTextColor</item>
        <item name="PermissionMsgColor">@color/uiTextColor</item>
        <item name="PermissionItemTextColor">@color/uiTextColor</item>
        <item name="PermissionButtonBackground">@drawable/login_bg_color</item>
        <item name="PermissionBackround">@drawable/btn_loading_shape</item>
        <item name="PermissionButtonTextColor">@android:color/white</item>
    </style>


</resources>